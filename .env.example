# Gemini API Key (Required for transaction classification and summaries)
GEMINI_API_KEY=your_gemini_api_key_here

# Services URLs (Used when running outside Docker; docker-compose overrides these automatically)
DATABASE_URL=postgresql://user:password@localhost:5432/alemeno_db
REDIS_URL=redis://localhost:6379/0
